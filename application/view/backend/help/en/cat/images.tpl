
<p>Images are used to represent category's contents. LC allows you to have unlimited number of images
 for one category, however only the <strong>Main</strong> image will be displayed in your store frontend. 
(Additional images can be used with custom template file modifications).</p>

<h3 id="add">Add an image</h3>
To add an image for a category:
<ul>
<li>Select a category in the category tree and click <strong>Images</strong> tab.</li> 
<li>In the Image section click <strong>Add new image</strong> and browse an image on your hard disk drive.</li>
<li>Click <strong>Upload</strong> to set an image.</li>
<li>(Then enter optional image title in the field provided).</li> 
<li>(Finally you can supply image titles in other languages if applicable. To do that click on Language to expand additional name 
fields.)</li>

<p class="note"><strong>Note:</strong> Upon upload, the image will automatically be resized to predefined sizes.
You can set image dimensions <ins>(and other details?)</ins> in the <strong>Settings</strong> section.</p>

<h3 id="sort">Reorder images</h3>
<p>To change the main image or sort images:</p>
<ul>
	<li>Hover the mouse cursor over an image.</li>
	<li>Then drag and drop an image to place it into appropriate position.</li>
</ul>

<h3 id="edit">Edit or Delete images</h3>

<p>To <strong>edit</strong> an image click a "pen" icon on the left:</p>
<img src="image/doc/categories/images/edit.bmp">
<p>When editing an image you can choose to upload an image as in the Add new image section above.</p>

<br \>
<br \>

<p>To <strong>delete</strong> an image click the "delete" icon on the left:</p>
<img src="image/doc/categories/images/delete.bmp">

{helpSeeAlso}
	{see cat.details}
	{see cat.attr}
	{see cat.filters}
{/helpSeeAlso}