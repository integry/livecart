<p>
	As in most e-commerce stores, with LiveCart you can organize products into categories. Categories can also contain subcategories which, in turn,
	may have their own subcategories. The number of subcategories and subcategory levels is unlimited. All the categories are displayed on the left 
	side of the screen in the tree-based structure.
</p>

<img src="image/doc/categories/category tree.bmp">

<p>	
	Category tree structure represents how all of the categories are arranged. (If a category "LCD monitors" is a <strong>subcategory</strong> of 
	"Monitors" then "Monitors" is a <strong>parent</strong> category for "LCD monitors"). At the top of the category tree is the 
	<strong>Root</strong> category which is a parent category for all the descending categories.
	(In the category tree you can easily manage your categories by selecting one of the options:)
</p>

<div class="tasks">
<fieldset>
<legend>Things you can do</legend>
<ul>	
	<li><a href="{self}#add">Create a new category</a></li>
	<li><a href="{self}#change order">Change category order</a></li>
	<li><a href="{self}#move">Move category</a></li>
	<li><a href="{self}#remove">Remove a category</a></li>

</ul>
</fieldset>
</div>

<h3 id="add">Create a new category</h3>

<p>
	If you want to add a <i>root</i> level category or create the very first category:
	<ul>
		<li>select LiveCart on the category tree</li>
		<li>click <strong>Add new category</strong> at the bottom of the category tree.
	</ul>
</p>

<p>
	If you want to add a <i>sub-category</i> for any existing category:
	<ul>
		<li>click on the desired category in the category tree </li>
		<li>click <strong>Add new category</strong>. </li>
	</ul>
</p>

<h3 id="change order">Change category order</h3>

<p>To change category order:</p>

<ul>
	<li>Select a category on the category tree.</li>
	<li>Use "Move category up" and "Move category down" buttons below the category tree:</li>
	<img src="image/doc/categories/move.bmp">
</ul>

<h3 id="move">Move category</h3>

<p>Yuo can move categories by dragging and dropping them directly in the category tree:</p>

<ul>
	<li>Click a category on the category tree.</li>
	<li>While holding the category drag it up or down to place into appropriate category:</li>
	<img src="image/doc/categories/drag_drop.bmp">
</ul>

<p class="note"><strong>Note</strong>: All of the contents are moved together with the category, including products, attributes, images, etc.</p>

<h3 id="remove">Remove a category</h3>

<p>To remove a category (<strong>including all its subcategories and products</strong>) select a category and click <i>Remove a cetegory</i>:</p>
<p><img src="image/doc/categories/remove.bmp"></p>

{helpSeeAlso}
	{see cat.details}
	{see products}
	{see cat.attr}
	{see cat.filters}
	{see cat.images}
{/helpSeeAlso}