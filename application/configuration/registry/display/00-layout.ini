[_layout]

THEME = "<?php '/' . implode(', ', eval('$themes = array(\'default\' => \'default\', \'none\' => \'none\'); if (file_exists(ClassLoader::getRealPath(\'application.view.theme\'))) { foreach (new DirectoryIterator(ClassLoader::getRealPath(\'application.view.theme\')) as $dir) { if ($dir->isDir() && !$dir->isDot()) { $themes[$dir->getFileName()] = $dir->getFileName(); } } } return $themes;')) . '/'; ?>"
LANG_SELECTION = /LANG_NAMES, LANG_FLAGS, LANG_DROPDOWN/
